# Optional: backend URL (defaults to http://127.0.0.1:8000)
# VITE_API_URL=http://127.0.0.1:8000

# Must match server ADMIN_API_KEY for inventory quantity edits (defaults to dev-admin)
# VITE_ADMIN_KEY=dev-admin
